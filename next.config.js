const path = require("path"); // Add this line at the top

/** @type {import('next').NextConfig} */
const nextConfig = {};

// Your code using `path` on line 4 (if applicable)

module.exports = nextConfig;
